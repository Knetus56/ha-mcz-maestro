__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
dist/
build/
.DS_Store
*.orig

# Config locale (jetons d'auth MCP en clair) et settings Claude Code locaux
# — jamais destinés à un repo public.
.mcp.json
.claude/

# Notes de projet perso / notes de session Claude (infos réseau/config
# locale) — jamais destinées à ce repo public, gardées uniquement en
# local pour référence.
PLUGIN_NOTES.md
CLAUDE.md

# Fichier source brut du logo MCZ récupéré par fredo — la copie utilisée
# comme icône du composant vit directement sous custom_components/mcz_maestro/
# (icon.png, brand/icon.png), committée volontairement malgré le risque de
# marque (décision explicite de fredo) ; ce fichier-ci reste hors repo,
# doublon inutile.
400x400ia-75.png
